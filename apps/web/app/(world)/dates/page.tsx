import { DatesPage } from "@/features/world/dates/DatesPage";

// Dates page — beautiful statistics + interactive letter composer with envelope
export default function DatesPageRoute() {
  return <DatesPage />;
}